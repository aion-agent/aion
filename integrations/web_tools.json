[
  {
    "name": "web_search",
    "description": "Search the web for information using Tavily API.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": { "type": "string" }
      },
      "required": ["query"]
    }
  }
]
